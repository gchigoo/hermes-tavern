doc_type: feature-checklist
feature: hermes-tavern-phase66-non-rp-allow-no-send
date: "2026-05-29"
status: implemented

summary: >
  Add a low-risk, test-only gateway dispatch regression proving that ordinary
  non-/rp Telegram messages with no active Tavern session pass through to Hermes
  Gateway and do not call the platform adapter send path.

steps:
  - id: S1
    name: "Add non-/rp allow no-send regression"
    description: >
      Tighten the existing non-/rp no-active-session gateway dispatch test to
      keep a fake gateway instance and assert its Telegram adapter recorded no
      sends after pre_gateway_dispatch returns allow.
    status: verified
    changed_file: "tests/test_hermes_tavern_gateway.py"
    result: "PASS - focused gateway dispatch test passed"

  - id: S2
    name: "Focused gateway tests"
    description: >
      Run the gateway hook and gateway dispatch test modules to verify the new
      no-send assertion and existing Telegram/mobile gateway contracts.
    verification_command: >
      python -m pytest tests/test_hermes_tavern_gateway.py tests/test_hermes_tavern_gateway_hook.py -q -o 'addopts='
    status: verified
    result: "PASS — controller rerun 2026-05-29; 19 passed in 0.44s"

  - id: S3
    name: "py_compile smoke check"
    description: >
      Compile the standalone shim, source modules, importer modules, and Tavern
      tests without changing runtime behavior.
    verification_command: >
      python -m py_compile conftest.py plugins/__init__.py plugins/hermes_tavern/__init__.py src/hermes_tavern/*.py src/hermes_tavern/importers/*.py tests/test_hermes_tavern_*.py
    status: verified
    result: "PASS — controller rerun 2026-05-29; no syntax errors"

  - id: S4
    name: "Full Tavern test suite"
    description: >
      Run the full standalone Tavern pytest glob and record any transient image
      failures without changing unrelated runtime code.
    verification_command: >
      python -m pytest tests/test_hermes_tavern_*.py -q -o 'addopts='
    status: verified
    result: >
      PASS — controller rerun 2026-05-29; 565 passed in 9.77s. Codex's own
      subprocess previously reported 8 transient image-test failures, but the
      controller-owned rerun is green.

  - id: S5
    name: "Checklist YAML validation"
    description: >
      Validate this checklist with the project YAML validator.
    verification_command: >
      python design/codestable/tools/validate-yaml.py --file design/codestable/features/2026-05-29-hermes-tavern-phase66-non-rp-allow-no-send/checklist.yaml
    status: verified
    result: "PASS — controller rerun 2026-05-29; 1 file passed, 0 failed"

implementation_notes: |
  - This phase is test-only; no runtime behavior changed.
  - The strengthened regression uses fake in-process gateway objects only.
  - No secrets, real provider calls, or network calls are introduced.
