doc_type: feature-checklist
status: accepted
feature: "2026-06-07-hermes-tavern-phase154-chapter-json-export"
date: "2026-06-07"
implementation_ready: true
summary: >
  Phase 154 adds one bounded /rp chapter export <chapter-id> JSON export
  command. S1 implemented chapter export by ID, S2 completed acceptance and
  architecture/root-design writeback. Full acceptance verified 2026-06-07:
  1115 Tavern tests passing, protected-files diff clean, ARCHITECTURE.md and
  HERMES_TAVERN_DESIGN.md updated.
  - hermes-tavern
  - chapter
  - novel
  - offline
  - export
  - json
implementation_readiness: >
  Architect profile (gpt-5.5) was rate-limited; controller materialized
  design/checklist artifacts following the proven Phase 149-153 export pattern.
  Executor profile (gpt-5.3-codex-spark) available. S1 implementation ready.
steps:
  - id: S1
    name: "S1: Implement chapter JSON export by ID"
    status: completed
    description: >
      Add /rp chapter export <chapter-id> under the existing chapter command family.
      Resolve one stored chapter by integer ID, read chapter metadata + associated
      scenes, build a JSON export payload, write one profile-safe UTF-8 JSON file,
      return file and quoted MEDIA output, update help/README visibility, and add
      focused offline tests.
    allowed_files:
      - src/hermes_tavern/runtime_novel.py
      - src/hermes_tavern/commands.py
      - README.md
      - tests/test_hermes_tavern_novel_runtime.py
      - tests/test_hermes_tavern_readme_docs.py
    prohibited_files:
      - run_agent.py
      - cli.py
      - gateway/run.py
      - src/hermes_tavern/runtime.py
      - src/hermes_tavern/runtime_lifecycle.py
      - src/hermes_tavern/runtime_sessions.py
      - src/hermes_tavern/runtime_assets.py
      - src/hermes_tavern/db.py
      - src/hermes_tavern/db_novel.py
      - src/hermes_tavern/prompt.py
      - src/hermes_tavern/runtime_prompt_modules.py
      - src/hermes_tavern/model_router.py
      - src/hermes_tavern/provider_bridge.py
      - src/hermes_tavern/adapters.py
      - src/hermes_tavern/runtime_generation.py
      - src/hermes_tavern/runtime_lore.py
      - src/hermes_tavern/runtime_persona.py
      - src/hermes_tavern/runtime_presets.py
      - "src/hermes_tavern/importers/**"
      - "plugins/**"
      - "build/lib/**"
    exact_scope:
      - "Add /rp chapter export <chapter-id> as a nested chapter subcommand."
      - "Resolve chapter ID via existing get_chapter(chapter_id) behavior."
      - "Reject not-found chapters with 'No novel chapter found: <id>'."
      - "Read chapter metadata fields (title, chapter_number, summary, status, project_id)."
      - "Read associated scenes via list_scenes(chapter_id) for scene metadata array."
      - "Build JSON payload with chapter metadata + scenes array."
      - "Write one UTF-8 JSON file under get_hermes_home()/plugins/hermes-tavern/exports/chapters."
      - 'Return bounded file: and quoted MEDIA:"<path>" output.'
      - "Update generated help and README Core commands for /rp chapter export <chapter-id>."
      - "Add focused tests: export by ID, not-found chapter, chapter with scenes, chapter without scenes, path containment, quoted paths with spaces, no chapter/scene mutation."
      - "Do not change chapter/scene lifecycle, schema, prompt compiler, provider/model routing, generation, credentials, content mode, safety, project archive, gateway/core files, plugin files, or build artifacts."
    verification:
      - command: "PYTHONDONTWRITEBYTECODE=1 python -m py_compile src/hermes_tavern/runtime_novel.py src/hermes_tavern/commands.py tests/test_hermes_tavern_novel_runtime.py tests/test_hermes_tavern_readme_docs.py"
        expected: "passes"
        actual: "passed"
      - command: "PYTHONDONTWRITEBYTECODE=1 python -m pytest tests/test_hermes_tavern_novel_runtime.py tests/test_hermes_tavern_readme_docs.py -q -o 'addopts=' -p no:cacheprovider"
        expected: "passes"
        actual: "248 passed in 10.43s"
      - command: "PYTHONDONTWRITEBYTECODE=1 python -m pytest tests/test_hermes_tavern_*.py -q -o 'addopts=' -p no:cacheprovider"
        expected: "passes"
        actual: "1115 passed in 47.36s"
      - command: "PYTHONDONTWRITEBYTECODE=1 python -m pytest -q -o 'addopts=' -p no:cacheprovider"
        expected: "passes when feasible"
        actual: ""
      - command: "git diff --name-only -- run_agent.py cli.py gateway/run.py src/hermes_tavern/runtime.py src/hermes_tavern/runtime_lifecycle.py src/hermes_tavern/runtime_sessions.py src/hermes_tavern/runtime_assets.py src/hermes_tavern/db.py src/hermes_tavern/db_novel.py src/hermes_tavern/prompt.py src/hermes_tavern/runtime_prompt_modules.py src/hermes_tavern/model_router.py src/hermes_tavern/provider_bridge.py src/hermes_tavern/adapters.py src/hermes_tavern/runtime_generation.py src/hermes_tavern/runtime_lore.py src/hermes_tavern/runtime_persona.py src/hermes_tavern/runtime_presets.py src/hermes_tavern/importers plugins build/lib"
        expected: "empty output"
        actual: "empty"
      - command: "git diff --check"
        expected: "passes"
        actual: "clean"
      - command: "python design/codestable/tools/validate-yaml.py --file design/codestable/features/2026-06-07-hermes-tavern-phase154-chapter-json-export/design.md --require doc_type --require status --require feature"
        expected: "passes"
        actual: "passed"
      - command: "python design/codestable/tools/validate-yaml.py --yaml-only --file design/codestable/features/2026-06-07-hermes-tavern-phase154-chapter-json-export/checklist.yaml"
        expected: "passes"
        actual: "passed"
  - id: S2
    name: "S2: Acceptance and architecture/root-design writeback"
    status: completed
    description: >
      S2 acceptance deferred to later cron tick. After S1 passes all controller
      verification, rerun gates, create acceptance report, update ARCHITECTURE.md
      and HERMES_TAVERN_DESIGN.md, and finalize checklist/design status.
    allowed_files:
      - design/codestable/features/2026-06-07-hermes-tavern-phase154-chapter-json-export/design.md
      - design/codestable/features/2026-06-07-hermes-tavern-phase154-chapter-json-export/checklist.yaml
      - design/codestable/features/2026-06-07-hermes-tavern-phase154-chapter-json-export/hermes-tavern-phase154-chapter-json-export-acceptance.md
      - design/codestable/architecture/ARCHITECTURE.md
      - design/HERMES_TAVERN_DESIGN.md
    prohibited_files:
      - README.md
      - "src/**"
      - "tests/**"
      - run_agent.py
      - cli.py
      - gateway/run.py
      - "plugins/**"
      - "build/lib/**"
    exact_scope:
      - "Rerun S1 verification before acceptance."
      - "Create Phase 154 acceptance only after verification passes."
      - "Update architecture/root design only for current /rp chapter export <chapter-id> behavior."
      - "Do not edit source, tests, README, plugin files, build artifacts, or protected core files during S2."
    verification:
      - command: "python design/codestable/tools/validate-yaml.py --file design/codestable/features/2026-06-07-hermes-tavern-phase154-chapter-json-export/design.md --require doc_type --require status --require feature"
        expected: "passes"
      - command: "python design/codestable/tools/validate-yaml.py --yaml-only --file design/codestable/features/2026-06-07-hermes-tavern-phase154-chapter-json-export/checklist.yaml"
        expected: "passes"
      - command: "PYTHONDONTWRITEBYTECODE=1 python -m pytest tests/test_hermes_tavern_novel_runtime.py tests/test_hermes_tavern_readme_docs.py -q -o 'addopts=' -p no:cacheprovider"
        expected: "passes"
      - command: "PYTHONDONTWRITEBYTECODE=1 python -m pytest tests/test_hermes_tavern_*.py -q -o 'addopts=' -p no:cacheprovider"
        expected: "passes"
      - command: "git diff --name-only -- README.md src tests run_agent.py cli.py gateway/run.py plugins build/lib"
        expected: "empty output during S2"
      - command: "git diff --check"
        expected: "passes"
risks:
  - id: R1
    risk: "Chapter ID may be invalid or not found."
    mitigation: "Reject with 'No novel chapter found: <id>' (same UX as chapter_inspect)."
  - id: R2
    risk: "Export payload may include too many scenes for very large chapters."
    mitigation: "list_scenes returns all scenes for a chapter; pagination is deferred to later phase if needed."
rollback: >
  Remove the chapter export branch/helpers, help and README literal, and focused
  tests. No DB migration or stored-data rollback is required.
notes:
  - "No architect pass was run (gpt-5.5 rate-limited). Controller materialized artifacts following the proven Phase 149-153 pattern."
  - "S1 implemented by Codex executor (gpt-5.3-codex-spark). S2 acceptance writeback completed by controller 2026-06-07."
