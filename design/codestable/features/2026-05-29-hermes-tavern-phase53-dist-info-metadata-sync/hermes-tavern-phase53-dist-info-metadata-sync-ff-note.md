---
doc_type: feature-ff-note
feature: hermes-tavern-phase53-dist-info-metadata-sync
date: 2026-05-29
requirement: ""
tags: [packaging, metadata, dist-info, setuptools]
---

## 做了什么
Phases 49–51 enriched pyproject.toml with classifiers, keywords, authors, license, and README, but the editable install was never refreshed, so dist-info METADATA stayed at 4 bare lines. Re-ran `pip install -e .` to regenerate the full METADATA file and added a test that catches future stale-dist-info scenarios.

## 改了哪些
- `hermes_tavern-0.1.0.dist-info/METADATA` (site-packages) — regenerated by `pip install -e .`; now includes Author, License, Keywords, 7 Classifiers, Description-Content-Type
- `tests/test_hermes_tavern_packaging.py:test_package_metadata_is_complete` — new test asserting Author, License, Keywords, Description-Content-Type, and Python/OSI classifiers are present

## 怎么验证的
`python -m pytest tests/test_hermes_tavern_*.py -q -o 'addopts='` — 451 passed (was 450; +1). py_compile and checklist YAML validator both clean.

## 顺手发现（可选，不阻塞）
- `README.md:30` Installation section says `pip install -e /path/to/hermes-tavern`; canonical standalone dev form is `pip install -e .` (already in the Development section) — inconsistency, not blocking
