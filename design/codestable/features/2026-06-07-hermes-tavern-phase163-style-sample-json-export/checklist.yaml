doc_type: feature-checklist
status: accepted
feature: "2026-06-07-hermes-tavern-phase163-style-sample-json-export"
date: "2026-06-07"
implementation_ready: true
summary: >
  Phase 163 adds one bounded /rp style sample export <style-sample-id> JSON export
  command. S1 implements style sample export by ID; S2 completes acceptance and
  architecture/root-design writeback (deferred to controller after S1
  verification passes).
  - hermes-tavern
  - style-sample
  - novel
  - offline
  - export
  - json
implementation_readiness: >
  Architect profile (gpt-5.5) was rate-limited; controller materialized
  design/checklist artifacts following the proven Phase 149-162 export pattern.
  Executor profile (gpt-5.3-codex-spark) available. S1 implementation ready.
steps:
  - id: S1
    name: "S1: Implement style sample JSON export by ID"
    status: completed
    description: >
      Add /rp style sample export <style-sample-id> under the existing style sample command family.
      Resolve one stored style sample by integer ID, read style sample metadata, build a
      JSON export payload, write one profile-safe UTF-8 JSON file, return file
      and quoted MEDIA output, update help/README visibility, and add focused
      offline tests.
    allowed_files:
      - src/hermes_tavern/runtime_novel.py
      - src/hermes_tavern/commands.py
      - README.md
      - tests/test_hermes_tavern_novel_runtime.py
      - tests/test_hermes_tavern_readme_docs.py
    prohibited_files:
      - run_agent.py
      - cli.py
      - gateway/run.py
      - src/hermes_tavern/runtime.py
      - src/hermes_tavern/runtime_lifecycle.py
      - src/hermes_tavern/runtime_sessions.py
      - src/hermes_tavern/runtime_assets.py
      - src/hermes_tavern/db.py
      - src/hermes_tavern/db_novel.py
      - src/hermes_tavern/prompt.py
      - src/hermes_tavern/runtime_prompt_modules.py
      - src/hermes_tavern/model_router.py
      - src/hermes_tavern/provider_bridge.py
      - src/hermes_tavern/adapters.py
      - src/hermes_tavern/runtime_generation.py
      - src/hermes_tavern/runtime_lore.py
      - src/hermes_tavern/runtime_persona.py
      - src/hermes_tavern/runtime_presets.py
      - "src/hermes_tavern/importers/**"
      - "plugins/**"
      - "build/lib/**"
    exact_scope:
      - "Add /rp style sample export <style-sample-id> as a nested style sample subcommand."
      - "Add 'export' subcommand branch to style_command() dispatcher after the 'delete' case."
      - "Resolve style sample ID via existing get_style_sample(style_sample_id) behavior."
      - "Reject not-found style sample with 'No style sample found: <id>'."
      - "Read style sample metadata fields (id, project_id, label, sample_text, created_at, updated_at)."
      - "Build JSON payload with style sample metadata keyed as 'style_sample_id'."
      - "Write one UTF-8 JSON file under get_hermes_home()/plugins/hermes-tavern/exports/style-samples."
      - 'Return bounded file: and quoted MEDIA:"<path>" output.'
      - "Update generated help (commands.py help_lines) for /rp style sample export <style-sample-id>."
      - "Update README Core commands table for style sample export."
      - "Add focused tests: export by ID, not-found style sample, non-positive ID, missing args, path containment, MEDIA output, no style sample mutation."
      - "Do not change style sample schema, project archive, prompt compiler, provider/model routing, generation, credentials, content mode, safety, gateway/core files, plugin files, or build artifacts."
      - "Do not edit db_novel.py (no new DB APIs needed; get_style_sample already exists)."
    verification:
      - command: "PYTHONDONTWRITEBYTECODE=1 python -m py_compile src/hermes_tavern/runtime_novel.py src/hermes_tavern/commands.py tests/test_hermes_tavern_novel_runtime.py tests/test_hermes_tavern_readme_docs.py"
        expected: "passes"
      - command: "PYTHONDONTWRITEBYTECODE=1 python -m pytest tests/test_hermes_tavern_novel_runtime.py tests/test_hermes_tavern_readme_docs.py -q -o 'addopts=' -p no:cacheprovider"
        expected: "passes"
      - command: "PYTHONDONTWRITEBYTECODE=1 python -m pytest tests/test_hermes_tavern_*.py -q -o 'addopts=' -p no:cacheprovider"
        expected: "passes"
      - command: "git diff --name-only -- run_agent.py cli.py gateway/run.py src/hermes_tavern/runtime.py src/hermes_tavern/runtime_lifecycle.py src/hermes_tavern/runtime_sessions.py src/hermes_tavern/runtime_assets.py src/hermes_tavern/db.py src/hermes_tavern/db_novel.py src/hermes_tavern/prompt.py src/hermes_tavern/runtime_prompt_modules.py src/hermes_tavern/model_router.py src/hermes_tavern/provider_bridge.py src/hermes_tavern/adapters.py src/hermes_tavern/runtime_generation.py src/hermes_tavern/runtime_lore.py src/hermes_tavern/runtime_persona.py src/hermes_tavern/runtime_presets.py src/hermes_tavern/importers plugins build/lib"
        expected: "empty output"
      - command: "git diff --check"
        expected: "passes"
  - id: S2
    name: "S2: Acceptance and architecture/root-design writeback"
    status: completed
    description: >
      S2 acceptance deferred to later cron tick/controller. After S1 passes
      all controller verification, rerun gates, create acceptance report,
      update ARCHITECTURE.md and HERMES_TAVERN_DESIGN.md, and finalize
      checklist/design status.
    allowed_files:
      - design/codestable/features/2026-06-07-hermes-tavern-phase163-style-sample-json-export/design.md
      - design/codestable/features/2026-06-07-hermes-tavern-phase163-style-sample-json-export/checklist.yaml
      - design/codestable/architecture/ARCHITECTURE.md
      - design/HERMES_TAVERN_DESIGN.md
    prohibited_files:
      - README.md
      - "src/**"
      - "tests/**"
      - run_agent.py
      - cli.py
      - gateway/run.py
      - "plugins/**"
      - "build/lib/**"
    exact_scope:
      - "Rerun S1 verification before acceptance."
      - "Create Phase 163 acceptance only after verification passes."
      - "Update architecture/root design only for current /rp style sample export <style-sample-id> behavior."
      - "Do not edit source, tests, README, plugin files, build artifacts, or protected core files during S2."
    verification:
      - command: "PYTHONDONTWRITEBYTECODE=1 python -m pytest tests/test_hermes_tavern_novel_runtime.py tests/test_hermes_tavern_readme_docs.py -q -o 'addopts=' -p no:cacheprovider"
        expected: "passes"
      - command: "PYTHONDONTWRITEBYTECODE=1 python -m pytest tests/test_hermes_tavern_*.py -q -o 'addopts=' -p no:cacheprovider"
        expected: "passes"
      - command: "git diff --name-only -- README.md src tests run_agent.py cli.py gateway/run.py plugins build/lib"
        expected: "empty output during S2"
      - command: "git diff --check"
        expected: "passes"
risks:
  - id: R1
    risk: "Style sample ID may be invalid or not found."
    mitigation: "Reject with 'No style sample found: <id>' (same UX as style_sample_inspect)."
  - id: R2
    risk: "Style sample sample_text may be large; export payload could be large."
    mitigation: "Sample text is stored as TEXT; no auto-summarization or truncation in export (same as existing inspect behavior)."
rollback: >
  Remove the style sample export branch, the style_sample_export function, help and README
  literal, and focused tests. No DB migration or stored-data rollback is required.
notes:
  - "No architect pass was run (gpt-5.5 rate-limited). Controller materialized artifacts following the proven Phase 149-162 pattern."
  - "S1 will be implemented by Codex executor (gpt-5.3-codex-spark). S2 acceptance writeback to be completed by controller."
  - "db_novel.py is in prohibited_files because get_style_sample already exists and no new DB APIs are needed."
