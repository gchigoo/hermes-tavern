doc_type: feature-checklist
status: implemented
feature: "2026-06-07-hermes-tavern-phase150-lorebook-json-export"
date: "2026-06-07"
implementation_ready: true
summary: >
  IMPLEMENTED - Adds one offline `/rp lore export <lorebook>` JSON export surface for
  stored lorebooks, with help/README visibility and focused tests.
tags:
  - hermes-tavern
  - lorebook
  - offline
implementation_readiness: >
  IMPLEMENTED - Codex architect validated S1 as ready, Codex executor implemented
  the bounded runtime/help/README/test slice, and the controller reran targeted,
  Tavern-wide, full, validator, protected-file, and whitespace gates. Acceptance
  report plus architecture/root-design currentization remain planned as S2.
steps:
  - id: S1
    name: "S1: Add lorebook JSON export command"
    status: implemented
    description: >
      Implement `/rp lore export <lorebook>` as a local offline JSON export for
      one stored lorebook, using existing lorebook lookup and entry-listing
      behavior, profile-safe export paths, generated help/README visibility, and
      focused regression tests.
    allowed_files:
      - src/hermes_tavern/runtime_lore.py
      - src/hermes_tavern/commands.py
      - README.md
      - tests/test_hermes_tavern_lore_runtime.py
      - tests/test_hermes_tavern_commands.py
      - tests/test_hermes_tavern_readme_docs.py
    prohibited_files:
      - run_agent.py
      - cli.py
      - gateway/run.py
    exact_scope:
      - "Add `/rp lore export <lorebook>` under the existing lore command family."
      - "Resolve lorebooks with existing `TavernStore.get_lorebook(...)`, including `last`."
      - "Write one UTF-8 JSON file under `get_hermes_home()/plugins/hermes-tavern/exports/lorebooks`."
      - "Prefer stored `lorebooks.raw_json` when it is a JSON object; otherwise build a normalized fallback from existing lorebook and entry rows."
      - "Return bounded `file:` and quoted `MEDIA:\"<path>\"` output."
      - "Update generated help and README Core commands."
      - "Add focused offline tests for raw export, fallback export, `last`, usage/not-found, quoted paths with spaces, export-directory containment, and no session/message mutation."
      - "Do not change schema, importers, lore matching, regex guards, prompt/debug/context-budget, provider/model routing, generation, retrieval/vectorization, credentials, content-mode, safety, project archives, plugins, build artifacts, or protected core files."
    verification:
      - command: "PYTHONDONTWRITEBYTECODE=1 python -m py_compile src/hermes_tavern/runtime_lore.py src/hermes_tavern/commands.py tests/test_hermes_tavern_lore_runtime.py tests/test_hermes_tavern_commands.py tests/test_hermes_tavern_readme_docs.py"
        expected: "passes"
        actual: "passed in controller on 2026-06-07"
      - command: "PYTHONDONTWRITEBYTECODE=1 python -m pytest tests/test_hermes_tavern_lore_runtime.py tests/test_hermes_tavern_commands.py tests/test_hermes_tavern_readme_docs.py -q -o 'addopts=' -p no:cacheprovider"
        expected: "passes"
        actual: "105 passed in controller on 2026-06-07"
      - command: "PYTHONDONTWRITEBYTECODE=1 python -m pytest tests/test_hermes_tavern_*.py -q -o 'addopts=' -p no:cacheprovider"
        expected: "passes"
        actual: "1080 passed in controller on 2026-06-07"
      - command: "PYTHONDONTWRITEBYTECODE=1 python -m pytest -q -o 'addopts=' -p no:cacheprovider"
        expected: "passes"
        actual: "1080 passed in controller on 2026-06-07"
      - command: "git diff --name-only -- run_agent.py cli.py gateway/run.py src/hermes_tavern/runtime.py src/hermes_tavern/prompt.py src/hermes_tavern/runtime_prompt_modules.py src/hermes_tavern/model_router.py src/hermes_tavern/provider_bridge.py src/hermes_tavern/adapters.py src/hermes_tavern/runtime_generation.py src/hermes_tavern/db.py src/hermes_tavern/db_novel.py src/hermes_tavern/importers plugins build/lib"
        expected: "empty output during S1 implementation"
        actual: "empty output in controller on 2026-06-07"
      - command: "git diff --check"
        expected: "passes"
        actual: "passed in controller on 2026-06-07"
    notes:
      - "Executor JSONL includes two harmless failed exploratory reads for nonexistent runtime_card.py/runtime_export.py and one failing targeted pytest run before executor self-repair; final executor and controller reruns passed."
  - id: S2
    name: "S2: Acceptance and architecture/root-design writeback"
    status: planned
    description: >
      Create the acceptance report, update architecture/root-design docs to make
      `/rp lore export <lorebook>` current behavior, rerun validators/tests/guards,
      and mark the feature accepted.
    allowed_files:
      - design/codestable/features/2026-06-07-hermes-tavern-phase150-lorebook-json-export/design.md
      - design/codestable/features/2026-06-07-hermes-tavern-phase150-lorebook-json-export/checklist.yaml
      - design/codestable/features/2026-06-07-hermes-tavern-phase150-lorebook-json-export/hermes-tavern-phase150-lorebook-json-export-acceptance.md
      - design/codestable/architecture/ARCHITECTURE.md
      - design/HERMES_TAVERN_DESIGN.md
    verification:
      - command: "python design/codestable/tools/validate-yaml.py --file design/codestable/features/2026-06-07-hermes-tavern-phase150-lorebook-json-export/hermes-tavern-phase150-lorebook-json-export-acceptance.md --require doc_type --require status --require feature"
        expected: "passes after acceptance report exists"
      - command: "python design/codestable/tools/validate-yaml.py --file design/codestable/features/2026-06-07-hermes-tavern-phase150-lorebook-json-export/design.md --require doc_type --require status --require feature"
        expected: "passes"
      - command: "python design/codestable/tools/validate-yaml.py --yaml-only --file design/codestable/features/2026-06-07-hermes-tavern-phase150-lorebook-json-export/checklist.yaml"
        expected: "passes"
      - command: "PYTHONDONTWRITEBYTECODE=1 python -m pytest tests/test_hermes_tavern_lore_runtime.py tests/test_hermes_tavern_commands.py tests/test_hermes_tavern_readme_docs.py -q -o 'addopts=' -p no:cacheprovider"
        expected: "passes"
      - command: "PYTHONDONTWRITEBYTECODE=1 python -m pytest -q -o 'addopts=' -p no:cacheprovider"
        expected: "passes when feasible"
acceptance:
  - id: AC1
    criteria: "`/rp lore export <lorebook>` writes one JSON file under the profile-safe lorebook export directory and returns a quoted MEDIA marker."
    status: planned
  - id: AC2
    criteria: "Valid stored raw lorebook JSON objects are exported unchanged except pretty formatting."
    status: planned
  - id: AC3
    criteria: "Invalid, missing, or non-object raw JSON uses a normalized fallback derived from stored lorebook and entry rows."
    status: planned
  - id: AC4
    criteria: "`last`, usage, not-found, quoted-path, path-containment, help, README, and no-mutation behavior are covered by focused offline tests."
    status: planned
checks:
  - id: C1
    name: "Offline export boundary"
    status: completed
    criteria: "No schema/importer/matcher/prompt/provider/generation/retrieval/archive/safety/protected-core files are changed."
    actual: "Protected-file reverse-scope guard returned empty output; S1 changed only allowed runtime/help/README/test files."
  - id: C2
    name: "Acceptance writeback"
    status: planned
    criteria: "Acceptance report and current architecture/root-design docs capture `/rp lore export <lorebook>` as implemented behavior."
verification_notes: |
  - Codex planning artifact: /tmp/hermes-tavern-architect-phase150-planning.jsonl
  - Codex S1 architect artifact: /tmp/hermes-tavern-architect-phase150-s1.jsonl
  - Codex executor artifact: /tmp/hermes-tavern-executor-phase150-s1.jsonl
  - Executor smoke artifact: /tmp/hermes-tavern-executor-phase150-smoke.jsonl
  - Controller verification on 2026-06-07: py_compile passed; targeted tests 105 passed; Tavern-wide tests 1080 passed; full pytest 1080 passed; validators passed; protected-file guard empty; git diff --check passed.
notes:
  - "Initial 2026-06-07 planning tick created draft/planned artifacts only; implementation was deferred until this tick."
  - "S1 implementation is complete and locally verified. S2 acceptance/writeback remains planned and should not broaden source/runtime behavior."
