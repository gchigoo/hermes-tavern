doc_type: feature-checklist
feature: "2026-06-04-hermes-tavern-phase127-project-brief"
date: "2026-06-04"
status: in_progress
summary: >
  Project Brief v1 is in progress. S1 added local project type and premise
  metadata persistence plus Markdown export visibility for novel projects. S2
  added runtime inspect/set/clear commands, project-info visibility, command
  help, README command literals, and focused command/docs tests. Prompt assembly,
  provider routing, content mode, outline editing, defaults, credentials,
  protected core files, architecture/root-design writeback, and acceptance
  remain out of scope until S3.

steps:
  - id: S1
    name: "S1: Persist project brief metadata"
    status: completed
    completed_at: "2026-06-04"
    result: >
      IMPLEMENTED - S1 added the local novel_project_briefs side table/index,
      project brief store helpers for type/premise set/get/clear, strict
      allowlisted project_type validation, missing-project errors, preservation
      of sibling fields across updates, deletion/no visible getter result after
      clearing the last field, and conditional Markdown export visibility after
      Summary. Controller added a small mechanical export polish so blank labels
      are omitted when only one brief field is present. Runtime commands,
      README/help, project info command visibility, architecture/root-design
      writeback, and acceptance remain deferred to S2/S3.
    description: >
      Add the novel_project_briefs side table, index, store helpers, type
      validation, and Markdown export visibility. Add focused novel DB tests for
      migration, set/update/get/clear, missing project, invalid type, export
      inclusion, and empty export omission.
    exit_signal: >
      Focused novel DB tests pass. Only src/hermes_tavern/db_novel.py and
      tests/test_hermes_tavern_novel_db.py need changes in this slice.
    verification:
      - command: "codex --profile architect exec --json --color never --disable image_generation --sandbox read-only --cd /Users/steven/Projects/hermes-tavern <Phase 127 S1 artifact prompt>"
        result: "passed; artifact at /tmp/hermes-tavern-architect-phase127-s1.jsonl ended with RESULT: ARCHITECT_ARTIFACT and selected S1 only"
      - command: "codex --profile executor exec --json --color never --disable image_generation --sandbox workspace-write --cd /Users/steven/Projects/hermes-tavern <Phase 127 S1 implementation prompt>"
        result: "completed; JSONL at /tmp/hermes-tavern-executor-phase127-s1.jsonl; no rate-limit hits or failed command_execution records"
      - command: "PYTHONDONTWRITEBYTECODE=1 python -m py_compile src/hermes_tavern/db_novel.py tests/test_hermes_tavern_novel_db.py"
        result: "passed (controller rerun)"
      - command: "PYTHONDONTWRITEBYTECODE=1 python -m pytest tests/test_hermes_tavern_novel_db.py -q -o 'addopts=' -p no:cacheprovider"
        result: "passed; 31 passed in 1.20s (controller rerun after mechanical export polish)"
      - command: "PYTHONDONTWRITEBYTECODE=1 python -m pytest -q -o 'addopts=' -p no:cacheprovider"
        result: "passed; 837 passed in 34.08s (controller rerun)"
      - command: "PYTHONDONTWRITEBYTECODE=1 python design/codestable/tools/validate-yaml.py --file design/codestable/features/2026-06-04-hermes-tavern-phase127-project-brief/design.md --require doc_type --require status --require feature"
        result: "passed (controller rerun)"
      - command: "PYTHONDONTWRITEBYTECODE=1 python design/codestable/tools/validate-yaml.py --yaml-only --file design/codestable/features/2026-06-04-hermes-tavern-phase127-project-brief/checklist.yaml"
        result: "passed after final S1 status update (controller rerun)"
      - command: "git diff --check"
        result: "passed"
      - command: "git diff --name-only -- run_agent.py cli.py gateway/run.py"
        result: "passed; no protected core file diffs"
      - command: "controller S1 diff allowlist/reverse-scope review"
        result: "passed; implementation touched db_novel.py and novel DB tests only, plus controller CodeStable status docs; no runtime, prompt, provider, content-mode, generation, gateway, README, or protected core files changed"

  - id: S2
    name: "S2: Add project brief command surface"
    status: completed
    completed_at: "2026-06-04"
    result: >
      IMPLEMENTED - S2 added /rp project brief inspect/type/premise command
      routing, active-project inspect fallback, explicit-id-only mutating
      commands, bounded usage/missing-project/invalid-type/no-active messages,
      project-info type/premise visibility, /rp help command-table lines, README
      Core commands literals, and focused runtime/help/docs tests. Controller
      made a small mechanical docs/test correction so mutating command literals
      use required <project-id> syntax, added an explicit no-active inspect
      regression, and reran targeted plus full verification from scratch. No
      db_novel.py, prompt modules, provider/model/credential, content-mode,
      generation, architecture/root-design, acceptance, or protected core files
      changed.
    description: >
      Add /rp project brief inspect/type/premise command routing, bounded usage
      and error messages, command-table help, README Core commands visibility,
      and focused runtime/help/docs tests.
    exit_signal: >
      /rp project brief commands work for normal and boundary cases, and command
      table plus README docs assertions pass.
    verification:
      - command: "codex --profile architect exec --json --color never --disable image_generation --sandbox read-only --cd /Users/steven/Projects/hermes-tavern <Phase 127 S2 artifact prompt>"
        result: "passed; artifact at /tmp/hermes-tavern-architect-phase127-s2.jsonl ended with RESULT: ARCHITECT_ARTIFACT and confirmed S2 may proceed unattended"
      - command: "codex --profile executor exec --json --color never --disable image_generation --sandbox workspace-write --cd /Users/steven/Projects/hermes-tavern <Phase 127 S2 implementation prompt>"
        result: "completed; JSONL at /tmp/hermes-tavern-executor-phase127-s2.jsonl; no rate-limit hits or failed command_execution records; executor reported focused tests green"
      - command: "PYTHONDONTWRITEBYTECODE=1 python -m py_compile src/hermes_tavern/runtime_novel.py src/hermes_tavern/commands.py tests/test_hermes_tavern_novel_runtime.py tests/test_hermes_tavern_commands.py tests/test_hermes_tavern_readme_docs.py"
        result: "passed (controller rerun after mechanical docs/test correction)"
      - command: "PYTHONDONTWRITEBYTECODE=1 python -m pytest tests/test_hermes_tavern_novel_runtime.py tests/test_hermes_tavern_commands.py tests/test_hermes_tavern_readme_docs.py -q -o 'addopts=' -p no:cacheprovider"
        result: "passed; 133 passed in 2.73s (controller rerun after no-active inspect regression)"
      - command: "PYTHONDONTWRITEBYTECODE=1 python -m pytest -q -o 'addopts=' -p no:cacheprovider"
        result: "passed; 855 passed in 33.29s (controller rerun)"
      - command: "git diff --name-only -- run_agent.py cli.py gateway/run.py"
        result: "passed; no protected core file diffs"

  - id: S3
    name: "S3: Verify and write architecture"
    status: pending
    description: >
      Run focused/relevant verification, update architecture/root design to mark
      project type and premise current, create acceptance evidence, and preserve
      deferred boundaries for outline, content mode, defaults, provider behavior,
      and other future metadata.
    exit_signal: >
      Architecture/root design and acceptance docs are updated after verification,
      and protected-core plus reverse-scope checks pass.
    verification:
      - command: "PYTHONDONTWRITEBYTECODE=1 python -m pytest tests/test_hermes_tavern_novel_db.py tests/test_hermes_tavern_novel_runtime.py tests/test_hermes_tavern_commands.py tests/test_hermes_tavern_readme_docs.py -q -o 'addopts=' -p no:cacheprovider"
      - command: "git diff --check"
      - command: "git diff --name-only -- run_agent.py cli.py gateway/run.py"

checks:
  - id: C1
    name: "C1: No active checklist precondition"
    status: passed
    result: >
      Passed before S1 execution: status search found Phase 127 as the only
      planned/pending active feature after Phase 126 had already been accepted.
    criteria: >
      Before implementation, no other feature checklist is planned, pending,
      draft, or in progress; Phase 127 is the only new planned feature.

  - id: C2
    name: "C2: Persistence contract"
    status: passed
    result: >
      Passed by controller-run novel DB tests: novel_project_briefs persists
      allowlisted project_type and premise_text locally, preserves sibling
      fields, clears/deletes visible-empty rows, raises bounded missing-project
      and invalid-type errors, and keeps invalid type attempts from mutating
      existing brief data.
    criteria: >
      Project brief metadata is stored locally in novel_project_briefs, validates
      project_type, preserves unspecified fields on update, handles clear
      operations, and raises bounded ValueError messages for missing projects or
      invalid type.

  - id: C3
    name: "C3: Command contract"
    status: passed
    result: >
      Passed by controller-run runtime/help/docs tests: /rp project brief supports
      explicit and active inspect, explicit-id type/premise set and clear,
      bounded usage for malformed or missing mutating IDs, missing-project and
      invalid-type messages, no-active inspect guidance, and README/help command
      literals. A reverse-scope regression confirms project brief type/premise
      are not included in /rp debug prompt or /rp debug context.
    criteria: >
      /rp project brief inspect/type/premise commands provide bounded normal,
      usage, missing-active-project, missing-project, invalid-type, and blank
      premise behavior.

  - id: C4
    name: "C4: Export and info visibility"
    status: passed
    result: >
      Passed across S1/S2 controller-run tests: S1 DB/export tests proved project
      Markdown export shows the Project Brief section only when type/premise is
      present and omits it when empty; S2 runtime tests prove /rp project info
      shows project_type and premise when present and omits both fields when no
      brief exists.
    criteria: >
      /rp project info and project Markdown export show type/premise when present,
      while empty projects omit the Project Brief export section.

  - id: C5
    name: "C5: Offline and local boundary"
    status: passed
    result: >
      Passed through S2: S1 changes are local SQLite/export helpers and offline
      tests; S2 changes are local runtime command/info/help/README surfaces and
      offline tests. No provider calls, network calls, credential storage,
      content-mode changes, model routing changes, prompt assembly, or generation
      behavior changes were added.
    criteria: >
      The feature has no provider calls, no network calls, no credential storage,
      no content-mode changes, no model routing changes, and no prompt assembly
      or generation behavior changes.

  - id: C6
    name: "C6: Reverse-scope guard"
    status: passed
    result: >
      Passed through S2 by diff allowlist, protected-core guard, and debug
      prompt/context no-leak regression. No protected core files changed, and the
      feature has not added outline editing/storage, canon_policy, default
      bindings, character/relationship state, cloud/collab, backend accounts,
      minors/underage paths, provider safety bypass, import/archive, trimming,
      summarization, vectorization, regex postprocessors, or project brief prompt
      injection.
    criteria: >
      No protected core files are changed, and no outline editing/storage,
      canon_policy, default card/preset/lorebook binding, character state,
      relationship state, cloud/collab, backend account, minors/underage path,
      provider safety bypass, project import, archive ZIP workflow, context
      trimming, summarization, vectorization, or regex postprocessor is added.

verification:
  - command: "PYTHONDONTWRITEBYTECODE=1 python -m pytest tests/test_hermes_tavern_novel_db.py tests/test_hermes_tavern_novel_runtime.py tests/test_hermes_tavern_commands.py tests/test_hermes_tavern_readme_docs.py -q -o 'addopts=' -p no:cacheprovider"
    owner: executor
  - command: "PYTHONDONTWRITEBYTECODE=1 python -m pytest tests/test_hermes_tavern_*.py -q -o 'addopts=' -p no:cacheprovider"
    owner: controller
  - command: "PYTHONDONTWRITEBYTECODE=1 python design/codestable/tools/validate-yaml.py --yaml-only --file design/codestable/features/2026-06-04-hermes-tavern-phase127-project-brief/checklist.yaml"
    owner: controller
  - command: "PYTHONDONTWRITEBYTECODE=1 python design/codestable/tools/validate-yaml.py --file design/codestable/features/2026-06-04-hermes-tavern-phase127-project-brief/design.md --require doc_type --require status --require feature"
    owner: controller
  - command: "git diff --check"
    owner: controller
  - command: "git diff --name-only -- run_agent.py cli.py gateway/run.py"
    owner: controller
