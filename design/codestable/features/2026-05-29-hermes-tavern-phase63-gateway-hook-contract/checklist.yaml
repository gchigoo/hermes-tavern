doc_type: feature-checklist
feature: hermes-tavern-phase63-gateway-hook-contract
date: "2026-05-29"
status: implemented

summary: >
  Add a test-only gateway hook contract regression around /rp command
  interception for Telegram/mobile entry. The phase preserves runtime behavior
  while checking that a minimal fake gateway event can dispatch /rp help and
  deliver a safe string response.

steps:
  - id: S1
    name: "Add minimal-event /rp gateway hook regression"
    description: >
      Extend tests/test_hermes_tavern_gateway.py with a contract test using a
      minimal Telegram-like event shape. The test asserts /rp help is swallowed
      by pre_gateway_dispatch, routed through the Tavern command path, and sent
      to the fake adapter as a non-empty string.
    status: verified
    changed_file: "tests/test_hermes_tavern_gateway.py"
    result: "PASS - focused gateway hook test passed"

  - id: S2
    name: "Focused gateway hook tests"
    description: >
      Run the gateway hook test module, including the new minimal-event command
      interception regression and the existing non-/rp allow-path checks.
    status: verified
    verification_command: >
      python -m pytest tests/test_hermes_tavern_gateway.py -q -o 'addopts='
    result: "PASS — controller rerun 2026-05-29; 7 passed in 0.19s"

  - id: S3
    name: "py_compile smoke check"
    description: >
      Compile the standalone shim, source, importer, and Tavern test files.
    status: verified
    verification_command: >
      python -m py_compile conftest.py plugins/__init__.py plugins/hermes_tavern/__init__.py src/hermes_tavern/*.py src/hermes_tavern/importers/*.py tests/test_hermes_tavern_*.py
    result: "PASS — controller rerun 2026-05-29; no syntax errors"

  - id: S4
    name: "Full Tavern test suite"
    description: >
      Run the full standalone Tavern pytest glob and record any transient image
      failures without changing unrelated runtime code.
    status: verified
    verification_command: >
      python -m pytest tests/test_hermes_tavern_*.py -q -o 'addopts='
    result: >
      PASS — controller rerun 2026-05-29; 561 passed in 9.87s. Codex's own
      subprocess previously reported 8 transient image-test failures, but the
      controller-owned rerun is green.

  - id: S5
    name: "Checklist YAML validation"
    description: >
      Validate this checklist with the project YAML validator.
    status: verified
    verification_command: >
      python design/codestable/tools/validate-yaml.py --file design/codestable/features/2026-05-29-hermes-tavern-phase63-gateway-hook-contract/checklist.yaml
    result: "PASS — controller rerun 2026-05-29; 1 file passed, 0 failed"

implementation_notes: |
  - This phase is test-only; no runtime behavior changed.
  - The new test uses fake in-process gateway objects only.
  - No secrets, real provider calls, or network calls are introduced.
