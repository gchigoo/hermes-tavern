doc_type: feature-checklist
feature: hermes-tavern-phase61-package-data-loader-contract
date: "2026-05-29"
status: implemented

summary: >
  Add a test-only hardening phase that validates Hermes Tavern's declared
  package-data resources are readable via importlib.resources from the canonical
  hermes_tavern package, and that plugin.yaml exposes the minimal loader
  metadata contract fields.

steps:
  - id: S1
    name: "Add declared package-data resource test"
    description: >
      Extend tests/test_hermes_tavern_packaging.py with a regression test that
      parses pyproject.toml, reads each declared hermes_tavern package-data
      resource through importlib.resources, and verifies plugin.yaml declares
      name, version, and entry_point.
    status: verified
    changed_file: "tests/test_hermes_tavern_packaging.py"
    result: "PASS - declared package-data resource test passed in focused run"

  - id: S2
    name: "Focused packaging test run"
    description: >
      Run the packaging test module to verify the Phase 61 package-data contract
      alongside existing standalone packaging checks.
    status: verified
    verification_command: >
      python -m pytest tests/test_hermes_tavern_packaging.py -q -o 'addopts='
    result: "PASS — controller rerun 2026-05-29; 14 passed in 0.12s"

  - id: S3
    name: "py_compile smoke check"
    description: >
      Compile the standalone shim, source, importer, and Tavern test files.
    status: verified
    verification_command: >
      python -m py_compile conftest.py plugins/__init__.py plugins/hermes_tavern/__init__.py src/hermes_tavern/*.py src/hermes_tavern/importers/*.py tests/test_hermes_tavern_*.py
    result: "PASS — controller rerun 2026-05-29; no syntax errors"

  - id: S4
    name: "Full Tavern test suite"
    description: >
      Run the full standalone Tavern pytest glob and record any transient
      failures without changing unrelated runtime code.
    status: verified
    verification_command: >
      python -m pytest tests/test_hermes_tavern_*.py -q -o 'addopts='
    result: >
      PASS — controller rerun 2026-05-29; 559 passed in 9.37s. Codex's own
      subprocess previously reported 8 transient image-test failures, but the
      controller-owned rerun is green.

  - id: S5
    name: "Checklist YAML validation"
    description: >
      Validate the Phase 61 checklist with the project YAML validator.
    status: verified
    verification_command: >
      python design/codestable/tools/validate-yaml.py --file design/codestable/features/2026-05-29-hermes-tavern-phase61-package-data-loader-contract/checklist.yaml
    result: "PASS — controller rerun 2026-05-29; 1 file passed, 0 failed"

implementation_notes: |
  - This phase is test-only; no runtime behavior changed.
  - The new test avoids provider calls, secrets, and network access.
  - The coverage is intentionally package-data focused to complement the
    existing entry_point string, version sync, and dynamic loader tests.
  - Full pytest was not green in this subprocess run due to unrelated image
    generation failures; no runtime code was changed to address them.
