doc_type: feature-checklist
feature: hermes-tavern-phase69-unmatched-platform-allow-no-send
date: "2026-05-29"
status: implemented

summary: >
  Add a low-risk, test-only gateway dispatch regression proving that ordinary
  non-/rp messages from a platform without a matching gateway adapter return
  allow and do not call existing adapter send or media delivery paths.

steps:
  - id: S1
    name: "Add unmatched-platform allow no-send regression"
    description: >
      Add a gateway dispatch test for a non-/rp event whose source platform is
      absent from the fake gateway adapters map, while a Telegram adapter still
      exists and must remain untouched.
    status: verified
    changed_file: "tests/test_hermes_tavern_gateway.py"
    result: "PASS - focused unmatched-platform gateway dispatch contract passed"

  - id: S2
    name: "Focused gateway tests"
    description: >
      Run the gateway dispatch and gateway hook modules to verify the new
      unmatched-platform no-send contract and existing Telegram/mobile gateway
      behavior.
    verification_command: >
      python -m pytest tests/test_hermes_tavern_gateway.py tests/test_hermes_tavern_gateway_hook.py -q -o 'addopts='
    status: verified
    result: "PASS — controller rerun 2026-05-29; 21 passed in 0.48s"

  - id: S3
    name: "py_compile smoke check"
    description: >
      Compile the standalone shim, source modules, importer modules, and Tavern
      tests without changing runtime/provider behavior.
    verification_command: >
      python -m py_compile conftest.py plugins/__init__.py plugins/hermes_tavern/__init__.py src/hermes_tavern/*.py src/hermes_tavern/importers/*.py tests/test_hermes_tavern_*.py
    status: verified
    result: "PASS — controller rerun 2026-05-29; no syntax errors"

  - id: S4
    name: "Full Tavern test suite"
    description: >
      Run the full standalone Tavern pytest glob and record any transient image
      failures honestly without changing unrelated runtime/provider code.
    verification_command: >
      python -m pytest tests/test_hermes_tavern_*.py -q -o 'addopts='
    status: verified
    result: >
      PASS — controller rerun 2026-05-29; 567 passed in 9.48s. Codex's own
      subprocess previously reported 8 transient image-test failures, but the
      controller-owned rerun is green.

  - id: S5
    name: "Checklist YAML validation"
    description: >
      Validate this checklist with the project YAML validator.
    verification_command: >
      python design/codestable/tools/validate-yaml.py --file design/codestable/features/2026-05-29-hermes-tavern-phase69-unmatched-platform-allow-no-send/checklist.yaml
    status: verified
    result: "PASS — controller rerun 2026-05-29; 1 file passed, 0 failed"

implementation_notes: |
  - This phase is test-only; no production runtime behavior changed.
  - The regression uses fake in-process gateway objects only.
  - No secrets, real provider calls, or network calls are introduced.
