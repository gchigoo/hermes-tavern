doc_type: feature-checklist
status: planned
feature: "2026-06-12-hermes-tavern-phase175-attention-status-sync-through-phase174"
date: "2026-06-12"
owner: codestable-cron
implementation_ready: false
summary: >
  Sync the mandatory CodeStable attention current-status line and static
  regression through accepted Phase 174.
tags:
  - hermes-tavern
  - codestable
  - attention
  - status-sync
  - docs
  - tests
allowed_files:
  - design/codestable/features/2026-06-12-hermes-tavern-phase175-attention-status-sync-through-phase174/design.md
  - design/codestable/features/2026-06-12-hermes-tavern-phase175-attention-status-sync-through-phase174/checklist.yaml
  - design/codestable/features/2026-06-12-hermes-tavern-phase175-attention-status-sync-through-phase174/phase175-acceptance.md
  - design/codestable/attention.md
  - tests/test_hermes_tavern_codestable_status.py
prohibited_files:
  - run_agent.py
  - cli.py
  - gateway/run.py
  - "gateway/**"
  - "src/**"
  - "plugins/**"
  - "build/lib/**"
  - README.md
  - design/HERMES_TAVERN_DESIGN.md
  - "design/codestable/architecture/**"
  - "design/codestable/roadmap/**"
  - "design/codestable/requirements/**"
  - "design/codestable/compound/**"
exact_scope:
  - "Replace only the existing '- Current status ...' bullet in design/codestable/attention.md with the Phase 1-174 accepted text from the design."
  - "Update only tests/test_hermes_tavern_codestable_status.py for the focused static status regression."
  - "Assert Phase 168 through Phase 174 short labels are present in the single current-status bullet."
  - "Assert stale standalone terminal 1-173 markers are absent while preserving the valid Phase 121-167 internal range."
  - "Do not add automatic phase discovery, feature-tree scanning, metadata extraction, generated status summaries, graph tooling, runtime behavior, root-design edits, architecture edits, README edits, or source changes."
steps:
  - id: S1
    name: "S1: Sync CodeStable attention status and regression"
    status: planned
    description: >
      Update the mandatory CodeStable attention current-status bullet through
      accepted Phase 174 and update the existing static docs regression to guard
      that status line.
    allowed_files:
      - design/codestable/attention.md
      - tests/test_hermes_tavern_codestable_status.py
    verification:
      - command: "PYTHONDONTWRITEBYTECODE=1 python -m py_compile tests/test_hermes_tavern_codestable_status.py"
        expected: "passes"
      - command: "PYTHONDONTWRITEBYTECODE=1 python -m pytest tests/test_hermes_tavern_codestable_status.py -q -o 'addopts=' -p no:cacheprovider"
        expected: "passes"
      - command: >
          rg -n "Current status \\(2026-06-12\\): All phases 1-174 accepted|Phase 168 image settings JSON export|Phase 169 project JSON export surface parity|Phase 170 export command-surface regression|Phase 171 root-design export parity|Phase 172 attention status sync through Phase 171|Phase 173 root-design section 17 import/export summary parity|Phase 174 attention status sync through Phase 173" design/codestable/attention.md tests/test_hermes_tavern_codestable_status.py
        expected: "finds all current Phase 175 status markers"
      - command: >
          python -c "from pathlib import Path; import re; s=[l for l in Path('design/codestable/attention.md').read_text(encoding='utf-8').splitlines() if re.match(r'^\\s*-\\s*Current status ', l)]; assert len(s)==1; line=s[0]; assert 'Current status (2026-06-12): All phases 1-174 accepted' in line; assert 'All phases 1-173 accepted' not in line; assert re.search(r'(?<!\\d)1[-\\u2013]173(?!\\d)', line) is None; assert 'Phase 121-167' in line"
        expected: "passes"
      - command: "git diff --name-only -- run_agent.py cli.py gateway src plugins README.md design/HERMES_TAVERN_DESIGN.md design/codestable/architecture design/codestable/roadmap design/codestable/requirements design/codestable/compound build/lib"
        expected: "empty output"
      - command: "git diff --check"
        expected: "passes"
  - id: S2
    name: "S2: Acceptance closeout"
    status: planned
    description: >
      After S1 implementation and controller verification, create the Phase 175
      acceptance report and finalize checklist/check statuses. Do not expand
      scope beyond the status-sync docs/test slice.
    allowed_files:
      - design/codestable/features/2026-06-12-hermes-tavern-phase175-attention-status-sync-through-phase174/checklist.yaml
      - design/codestable/features/2026-06-12-hermes-tavern-phase175-attention-status-sync-through-phase174/phase175-acceptance.md
checks:
  - id: C1
    name: "Focused CodeStable status regression"
    command: "PYTHONDONTWRITEBYTECODE=1 python -m pytest tests/test_hermes_tavern_codestable_status.py -q -o 'addopts=' -p no:cacheprovider"
    status: pending
    expected: "passes"
  - id: C2
    name: "Current Phase 175 status markers"
    command: >
      rg -n "Current status \\(2026-06-12\\): All phases 1-174 accepted|Phase 168 image settings JSON export|Phase 169 project JSON export surface parity|Phase 170 export command-surface regression|Phase 171 root-design export parity|Phase 172 attention status sync through Phase 171|Phase 173 root-design section 17 import/export summary parity|Phase 174 attention status sync through Phase 173" design/codestable/attention.md tests/test_hermes_tavern_codestable_status.py
    status: pending
    expected: "finds all current Phase 175 status markers"
  - id: C3
    name: "Stale terminal status guard"
    command: >
      python -c "from pathlib import Path; import re; s=[l for l in Path('design/codestable/attention.md').read_text(encoding='utf-8').splitlines() if re.match(r'^\\s*-\\s*Current status ', l)]; assert len(s)==1; line=s[0]; assert 'Current status (2026-06-12): All phases 1-174 accepted' in line; assert 'All phases 1-173 accepted' not in line; assert re.search(r'(?<!\\d)1[-\\u2013]173(?!\\d)', line) is None; assert 'Phase 121-167' in line"
    status: pending
    expected: "passes"
  - id: C4
    name: "Protected paths unchanged"
    command: "git diff --name-only -- run_agent.py cli.py gateway src plugins README.md design/HERMES_TAVERN_DESIGN.md design/codestable/architecture design/codestable/roadmap design/codestable/requirements design/codestable/compound build/lib"
    status: pending
    expected: "empty output"
  - id: C5
    name: "Whitespace check"
    command: "git diff --check"
    status: pending
    expected: "passes"
  - id: C6
    name: "CodeStable artifact validation"
    command: >
      PYTHONDONTWRITEBYTECODE=1 python design/codestable/tools/validate-yaml.py --file design/codestable/features/2026-06-12-hermes-tavern-phase175-attention-status-sync-through-phase174/design.md --require doc_type --require status --require feature && PYTHONDONTWRITEBYTECODE=1 python design/codestable/tools/validate-yaml.py --file design/codestable/features/2026-06-12-hermes-tavern-phase175-attention-status-sync-through-phase174/checklist.yaml --yaml-only --require doc_type --require status --require feature
    status: pending
    expected: "passes"
risks:
  - "The status line can become too long; keep it to one bullet and do not create generated phase-summary machinery."
  - "The stale-marker guard must reject only standalone terminal 1-173 markers and must preserve the valid Phase 121-167 internal range."
  - "Do not treat old accepted feature artifacts as active blockers; this phase updates current startup status only."
  - "Do not touch runtime, source, plugin, provider, generation, import/export payload, root-design, architecture, README, roadmap, requirement, compound, graph, cloud/archive sync, minors/underage, or safety-bypass behavior."
