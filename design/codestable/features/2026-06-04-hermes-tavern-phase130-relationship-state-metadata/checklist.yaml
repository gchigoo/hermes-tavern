doc_type: feature-checklist
feature: "2026-06-04-hermes-tavern-phase130-relationship-state-metadata"
date: "2026-06-05"
status: accepted
summary: >
  ACCEPTED - Relationship State Metadata v1 adds local project-scoped
  relationship notes backed by `novel_relationship_states`, managed through
  `/rp relationship ...` CRUD/list commands, visible as optional `## Relationships`
  Markdown export metadata, and verified to remain outside prompt/debug/provider/
  generation/context-budget/vectorization/retrieval paths. S1 implementation and
  S2 acceptance/writeback are complete with controller verification.

steps:
  - id: S1
    name: "S1: Implementation"
    status: completed
    completed_at: "2026-06-05"
    result: >
      IMPLEMENTED - S1 added local project-scoped relationship-state persistence,
      `/rp relationship add/list/inspect/update/delete`, help/README command
      literals, optional Markdown export visibility, and focused DB/runtime/docs
      tests. Controller verified py_compile, focused tests, full pytest, protected
      core diff guard, and whitespace checks before S2 acceptance.
    description: >
      Add local project-scoped relationship-state metadata persistence, the
      `/rp relationship ...` command family, help/README command literals,
      optional Markdown export visibility, and focused DB/runtime/docs tests.
      Keep the implementation inside the allowed plugin/source/docs/test files
      and avoid prompt/provider/model/content-mode/generation paths.
    allowed_files:
      - src/hermes_tavern/db_novel.py
      - src/hermes_tavern/runtime_novel.py
      - src/hermes_tavern/runtime.py
      - src/hermes_tavern/commands.py
      - README.md
      - tests/test_hermes_tavern_novel_db.py
      - tests/test_hermes_tavern_novel_runtime.py
      - tests/test_hermes_tavern_commands.py
      - tests/test_hermes_tavern_readme_docs.py
    exit_signal: >
      Relationship-state persistence, commands, help/docs, export visibility,
      and no-leak tests pass in focused verification; no protected core,
      prompt, provider, router, credential, content-mode, generation,
      retrieval/vectorization, cloud/collab, minors/underage, or safety-bypass
      file changes are present.
    verification:
      - command: >-
          codex --profile architect exec --json --color never --sandbox read-only --cd /Users/steven/Projects/hermes-tavern <Phase 130 S1 artifact prompt>
        result: "architect artifact available from prior tick at /tmp/hermes-tavern-architect-phase130-s1.jsonl; RESULT: PLANNING_ARTIFACT → READY"
      - command: >-
          codex --profile executor exec --json --color never --disable image_generation --sandbox workspace-write --cd /Users/steven/Projects/hermes-tavern <Phase 130 S1 implementation prompt>
        result: "2026-06-05 executor succeeded; EXIT=0; JSONL at /tmp/hermes-tavern-executor-phase130-s1-rerun2.jsonl"
      - command: >-
          PYTHONDONTWRITEBYTECODE=1 python -m py_compile src/hermes_tavern/db_novel.py src/hermes_tavern/runtime_novel.py src/hermes_tavern/runtime.py src/hermes_tavern/commands.py tests/test_hermes_tavern_novel_db.py tests/test_hermes_tavern_novel_runtime.py tests/test_hermes_tavern_commands.py tests/test_hermes_tavern_readme_docs.py
        result: "passed (no errors)"
      - command: >-
          PYTHONDONTWRITEBYTECODE=1 python -m pytest tests/test_hermes_tavern_novel_db.py tests/test_hermes_tavern_novel_runtime.py tests/test_hermes_tavern_commands.py tests/test_hermes_tavern_readme_docs.py -q -o 'addopts=' -p no:cacheprovider
        result: "205 passed in 5.70s during S2 controller rerun"
      - command: >-
          PYTHONDONTWRITEBYTECODE=1 python -m pytest tests/test_hermes_tavern_*.py -q -o 'addopts=' -p no:cacheprovider
        result: "896 passed in 41.70s during S2 controller rerun"
      - command: >-
          PYTHONDONTWRITEBYTECODE=1 python -m pytest -q -o 'addopts=' -p no:cacheprovider
        result: "896 passed in 42.08s during S2 controller rerun"
      - command: >-
          git diff --name-only -- run_agent.py cli.py gateway/run.py src/hermes_tavern/runtime_prompt_modules.py src/hermes_tavern/prompt.py src/hermes_tavern/runtime_debug.py src/hermes_tavern/model_router.py src/hermes_tavern/provider_bridge.py src/hermes_tavern/adapters.py src/hermes_tavern/runtime_model.py src/hermes_tavern/runtime_generation.py
        result: "empty (no protected-core/prompt/provider/generation changes)"
      - command: "git diff --check"
        result: "passed (no whitespace issues)"

  - id: S2
    name: "S2: Verify/Accept/Write Architecture"
    status: completed
    completed_at: "2026-06-05"
    result: >
      ACCEPTED - S2 created the Phase 130 acceptance report, promoted the feature
      design/checklist status, and wrote current relationship-state metadata
      behavior into architecture and root design docs. Controller reran py_compile,
      focused tests, Tavern glob tests, full pytest, frontmatter/YAML validators,
      protected-core/reverse-scope checks, and whitespace checks before marking
      the phase accepted.
    description: >
      Run focused, plugin, full, frontmatter/YAML, reverse-scope, and whitespace
      verification. Write the Phase 130 acceptance report, update this checklist
      and design status only after acceptance, and write current relationship
      state metadata behavior into architecture and root design docs.
    allowed_files:
      - design/codestable/features/2026-06-04-hermes-tavern-phase130-relationship-state-metadata/design.md
      - design/codestable/features/2026-06-04-hermes-tavern-phase130-relationship-state-metadata/checklist.yaml
      - design/codestable/features/2026-06-04-hermes-tavern-phase130-relationship-state-metadata/hermes-tavern-phase130-relationship-state-metadata-acceptance.md
      - design/codestable/architecture/ARCHITECTURE.md
      - design/HERMES_TAVERN_DESIGN.md
    exit_signal: >
      Acceptance report and architecture/root-design writeback are complete, the
      checklist is accepted, and verification confirms the feature remains local
      metadata/export-only.
    verification:
      - command: >-
          codex --profile architect exec --json --color never --sandbox read-only --cd /Users/steven/Projects/hermes-tavern <Phase 130 S2 artifact prompt>
        result: "passed; JSONL at /tmp/hermes-tavern-architect-phase130-s2.jsonl; RESULT: PLANNING_ARTIFACT → READY"
      - command: >-
          codex --profile executor exec --json --color never --disable image_generation --sandbox workspace-write --cd /Users/steven/Projects/hermes-tavern <Phase 130 S2 docs/status prompt>
        result: "passed; JSONL at /tmp/hermes-tavern-executor-phase130-s2.jsonl; executor drafted docs/status writeback for controller finalization"
      - command: >-
          python design/codestable/tools/validate-yaml.py --file design/codestable/features/2026-06-04-hermes-tavern-phase130-relationship-state-metadata/design.md --require doc_type --require status --require feature
        result: "passed after controller final status edits"
      - command: >-
          python design/codestable/tools/validate-yaml.py --file design/codestable/features/2026-06-04-hermes-tavern-phase130-relationship-state-metadata/hermes-tavern-phase130-relationship-state-metadata-acceptance.md --require doc_type --require status --require feature
        result: "passed after controller final status edits"
      - command: >-
          python design/codestable/tools/validate-yaml.py --yaml-only --file design/codestable/features/2026-06-04-hermes-tavern-phase130-relationship-state-metadata/checklist.yaml
        result: "passed after controller final status edits"

checks:
  - id: C1
    name: "C1: Persistence contract"
    status: completed
    completed_at: "2026-06-05"
    result: >
      PASSED - `novel_relationship_states` migration, project foreign key/index,
      and CRUD/list helpers are implemented and covered by focused DB tests for
      valid projects/IDs, missing projects/IDs, blank labels, and blank state text.
    criteria: >
      Migration creates the relationship-state table and project index; CRUD/list
      helpers handle valid projects/IDs, missing projects/IDs, blank labels, and
      blank state text with bounded behavior.

  - id: C2
    name: "C2: Command contract"
    status: completed
    completed_at: "2026-06-05"
    result: >
      PASSED - `/rp relationship add/list/inspect/update/delete` command literals
      and handlers are present, return bounded normal/error messages, and are
      visible through help/README guards.
    criteria: >
      `/rp relationship add <project-id> <label> <state...>`,
      `/rp relationship list [project-id]`,
      `/rp relationship inspect <relationship-id>`,
      `/rp relationship update <relationship-id> <state...>`, and
      `/rp relationship delete <relationship-id>` return compact normal and
      error messages and are visible in `/rp help`.

  - id: C3
    name: "C3: Markdown export"
    status: completed
    completed_at: "2026-06-05"
    result: >
      PASSED - Markdown export emits optional `## Relationships` only when rows
      exist, orders it after Style Guide and before Chapters, and omits it for
      projects with no relationship-state rows.
    criteria: >
      Project Markdown export includes `## Relationships` only when relationship
      states exist, orders it after Style Guide and before Chapters, and omits it
      for projects without relationship states.

  - id: C4
    name: "C4: Offline no-leak boundary"
    status: completed
    completed_at: "2026-06-05"
    result: >
      PASSED - Focused no-leak tests confirm distinctive relationship-state text
      stays out of `/rp debug prompt` and `/rp debug context`; protected prompt,
      provider, model routing, content mode, generation, vectorization/retrieval,
      credentials, and summarization paths are unchanged.
    criteria: >
      Relationship-state text does not appear in `/rp debug prompt` or
      `/rp debug context`; prompt module selection, provider calls, model routing,
      content mode, credentials, generation, vectorization/retrieval, and
      summarization behavior remain unchanged.

  - id: C5
    name: "C5: Reverse-scope verification"
    status: completed
    completed_at: "2026-06-05"
    result: >
      PASSED - Controller reverse-scope checks found no protected core,
      prompt/provider/model/generation, credential, cloud/collab, minors/underage,
      safety-bypass, source/test/README, or build-artifact changes in S2.
    criteria: >
      No protected core files, build artifacts, provider/model/generation files,
      credential paths, cloud/collab paths, minors/underage paths, or
      provider-safety bypass behavior are changed.
